# requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# CHANGE 1: mlflow → mlflow-skinny
# Full mlflow drags in: Flask, SQLAlchemy, Alembic, Werkzeug, GitPython,
# boto3, google-cloud-storage, protobuf, and 30+ additional packages that
# the inference service NEVER uses. mlflow-skinny keeps only the client,
# pyfunc loader, model registry client, and artifact downloader.
# Your load_mlflow() uses: mlflow.pyfunc.load_model() + download_artifacts()
# + MlflowClient — all available in mlflow-skinny.
# Estimated saving: 200–400 MB of installed package size.
# ─────────────────────────────────────────────────────────────────────────────
mlflow-skinny==2.17.0       # was: mlflow==2.17.0

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.0
gunicorn==23.0.0
python-multipart==0.0.18

# ── Validation / config ───────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# ── ML / inference ────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2         # pinned to latest stable; 1.7.x same approach
lightgbm==4.5.0
joblib==1.4.2

# ── Templating ────────────────────────────────────────────────────────────────
jinja2==3.1.4               # keep: used explicitly in main.py for templates

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-client==0.21.1

# ─────────────────────────────────────────────────────────────────────────────
# CHANGE 2: removed explicit pins that are transitive deps already
# (installed automatically by mlflow-skinny or fastapi — no need to declare)
#
#   requests==2.32.3   → pulled in by mlflow-skinny (httpx dep chain)
#   pyyaml==6.0.2      → pulled in by mlflow-skinny
#
# Removing them from this file does NOT remove them from the image —
# they still install as transitive deps. But it removes the version-pin
# conflict risk and keeps requirements.txt as the canonical list of
# packages YOUR code directly imports.
# ─────────────────────────────────────────────────────────────────────────────

# ucimlrepo==0.0.7  # training only — keep commented
# scipy==1.14.1     # not needed at inference


# fastapi==0.115.6
# uvicorn[standard]==0.32.0
# gunicorn==23.0.0

# python-multipart==0.0.18
# pydantic==2.9.2
# pydantic-settings==2.6.0

# pandas==2.2.3
# numpy==1.26.4
# # scipy==1.14.1
# scikit-learn==1.5.2
# lightgbm==4.5.0
# joblib==1.4.2

# jinja2==3.1.4
# requests==2.32.3
# prometheus-client==0.21.1
# python-dotenv==1.0.1

# # ucimlrepo==0.0.7
# pyyaml==6.0.2
# mlflow==2.17.0